
ALGORITHM triParInsertion
VAR
    arr : ARRAY_OF INTEGER[15];
    i,j,elem:INTEGER;
BEGIN
    FOR i FROM 1 TO arr.length STEP 1  DO
        elem=arr[i];
        j=i;
        WHILE (j>0 and arr[j-1]) DO
            arr[j]=arr[j-1];
            j=j-1;
        END_WHILE
    END_FOR
    
END